

'use strict';

/**
 * Shows loading screen, which is displayed until promise is not resolved or rejected
 * @param canvas {HTMLCanvasElement}
 * @param promise {Promise}
 */
export default function showLoading(canvas, promise) {
    const context = canvas.getContext('2d');
}